<UserControl
    x:Class="Axphi.WindowsComponents.MainWindow.TitleBar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:comp="clr-namespace:Axphi.Components"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ws="https://schemas.elecho.dev/wpfsuite"
    mc:Ignorable="d">

    <Grid d:Width="500">
        <StackPanel
            Margin="4,0,0,0"
            Orientation="Horizontal"
            WindowChrome.IsHitTestVisibleInChrome="True">
            <TextBlock VerticalAlignment="Center" Text="LOGO" />
            <ws:Menu Margin="4" VerticalAlignment="Center">
                <ws:MenuItem Header="File">
                    <ws:MenuItem Command="{Binding FileActions.SaveChartCommand}" Header="Save" />
                </ws:MenuItem>
                <ws:MenuItem Header="Debugging">
                    <ws:MenuItem Command="{Binding LoadDemoChartCommand}" Header="Load demo chart" />
                    <ws:MenuItem Command="{Binding LoadDemoChart2Command}" Header="Load demo chart 2" />
                </ws:MenuItem>
            </ws:Menu>
        </StackPanel>
        <StackPanel
            Margin="4"
            HorizontalAlignment="Right"
            Orientation="Horizontal">
            <comp:WindowControl />
        </StackPanel>
    </Grid>
</UserControl>